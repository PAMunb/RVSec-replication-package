/*
 * This file was automatically generated by EvoSuite
 * Sat Apr 16 13:25:46 GMT 2022
 */

package org.apache.activemq.artemis.utils;

import org.junit.Test;
import static org.junit.Assert.*;
import java.util.concurrent.TimeUnit;
import org.apache.activemq.artemis.utils.ReusableLatch;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true, useJEE = true) 
public class ReusableLatch_ESTest extends ReusableLatch_ESTest_scaffolding {

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      ReusableLatch reusableLatch0 = new ReusableLatch(0);
      reusableLatch0.countDown();
      reusableLatch0.setCount((-1591));
      reusableLatch0.countDown(0);
      reusableLatch0.setCount(0);
      reusableLatch0.countDown();
      TimeUnit timeUnit0 = TimeUnit.MINUTES;
      TimeUnit timeUnit1 = TimeUnit.NANOSECONDS;
      reusableLatch0.await((-1L), timeUnit1);
      reusableLatch0.await((long) 0, timeUnit0);
      reusableLatch0.countDown(0);
      reusableLatch0.await(1L);
      reusableLatch0.await((-1L));
      reusableLatch0.await();
      reusableLatch0.setCount(0);
      reusableLatch0.await();
      reusableLatch0.countDown();
      reusableLatch0.countDown();
      reusableLatch0.await();
      reusableLatch0.countDown(0);
      reusableLatch0.await(1L);
      reusableLatch0.getCount();
      reusableLatch0.countUp();
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      ReusableLatch reusableLatch0 = new ReusableLatch(0);
      reusableLatch0.countDown();
      reusableLatch0.await();
      reusableLatch0.setCount(0);
      TimeUnit timeUnit0 = TimeUnit.MICROSECONDS;
      reusableLatch0.await((-1650L), timeUnit0);
      reusableLatch0.countUp();
      TimeUnit timeUnit1 = TimeUnit.DAYS;
      reusableLatch0.await((-1650L), timeUnit1);
      reusableLatch0.setCount(0);
      reusableLatch0.setCount(0);
      reusableLatch0.countDown(288);
      reusableLatch0.setCount(1174);
      reusableLatch0.countUp();
      TimeUnit timeUnit2 = TimeUnit.NANOSECONDS;
      reusableLatch0.await((long) 0, timeUnit2);
      reusableLatch0.countDown(1174);
      reusableLatch0.countDown(288);
      reusableLatch0.await((long) 1174);
      reusableLatch0.countDown(1174);
  }

  @Test(timeout = 4000)
  public void test2()  throws Throwable  {
      ReusableLatch reusableLatch0 = new ReusableLatch();
      reusableLatch0.countDown();
      reusableLatch0.await();
      TimeUnit timeUnit0 = TimeUnit.MINUTES;
      reusableLatch0.await(1L, timeUnit0);
  }

  @Test(timeout = 4000)
  public void test3()  throws Throwable  {
      ReusableLatch reusableLatch0 = new ReusableLatch();
      reusableLatch0.await();
      reusableLatch0.countDown();
      reusableLatch0.countUp();
      reusableLatch0.getCount();
      TimeUnit timeUnit0 = TimeUnit.NANOSECONDS;
      reusableLatch0.await(2262L, timeUnit0);
      reusableLatch0.countUp();
      reusableLatch0.countUp();
      reusableLatch0.countDown();
      reusableLatch0.countDown();
      reusableLatch0.countDown();
      reusableLatch0.await((-146L));
      reusableLatch0.await(3320L);
      reusableLatch0.await();
      reusableLatch0.countUp();
      reusableLatch0.setCount((-1794));
      reusableLatch0.getCount();
      reusableLatch0.countUp();
      reusableLatch0.setCount((-1794));
      reusableLatch0.await(0L, timeUnit0);
      TimeUnit timeUnit1 = TimeUnit.DAYS;
      reusableLatch0.await(0L, timeUnit1);
      TimeUnit timeUnit2 = TimeUnit.SECONDS;
      reusableLatch0.await(0L, timeUnit2);
  }

  @Test(timeout = 4000)
  public void test4()  throws Throwable  {
      ReusableLatch reusableLatch0 = new ReusableLatch((-808));
      reusableLatch0.await((long) (-808));
      TimeUnit timeUnit0 = TimeUnit.MILLISECONDS;
      reusableLatch0.await((long) (-808), timeUnit0);
      reusableLatch0.setCount((-733));
      reusableLatch0.setCount(0);
      reusableLatch0.getCount();
      reusableLatch0.await((long) (-733));
      TimeUnit timeUnit1 = TimeUnit.HOURS;
      reusableLatch0.await(1L, timeUnit1);
      reusableLatch0.getCount();
      reusableLatch0.await();
      reusableLatch0.await((long) (-808));
      reusableLatch0.await();
      reusableLatch0.countDown((-918));
      reusableLatch0.countDown((-918));
      reusableLatch0.await(0L);
      reusableLatch0.countDown(0);
      reusableLatch0.countDown(0);
      reusableLatch0.countUp();
      reusableLatch0.await((long) (-808));
      reusableLatch0.getCount();
      reusableLatch0.getCount();
      reusableLatch0.getCount();
      reusableLatch0.countDown();
      reusableLatch0.await();
  }
}
